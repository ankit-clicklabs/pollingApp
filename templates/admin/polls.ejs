<%- include ../partials/header %>
<%- include partials/nav %>
<div class="container">
    <div class="row">
        <div class="col-md-6">
            <%
            var message= message || '';
            if (message.length > 0) { %>
            <div class="alert alert-success"><%= message %></div>
            <% }  %>



            <% for(var i=0;i<polls.length;i++){ %>
            <div class="poll well well-lg" data-id="Gp3xZ3yX7mTS4LtM4">

                <h3 class="text-center"><%= polls[i].question %>?</h3>

                <% for(var j=0;j<polls[i].answers.length;j++){ %>
                <a href="#" class="vote btn btn-primary btn-block" data-id="0"><%= polls[i].answers[j].title %>
                    <span class="votes pull-right"><%= polls[i].answers[j].votes %></span>
                    <span class="text"></span>
                </a>
                <% } %>


            </div>
            <% } %>
        </div>
    </div>
</div>
<%- include ../partials/footer %>
